tasks:
  - name: Start Server
    init: cd server && npm install
    command: npm run dev
  - name: Start Client
    init: cd client && npm install
    command: npm start

ports:
  - port: 3000
    onOpen: open-preview
  - port: 4000
    onOpen: open-preview
